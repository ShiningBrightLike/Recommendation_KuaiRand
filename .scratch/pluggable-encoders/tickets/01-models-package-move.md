## 父 Spec

#{{PARENT}}

## 交付内容

把现有的四个模型构建器（逻辑回归、共享底层、单任务、MMoE）与专家混合层搬进 models 包，并引入按名称查找的注册表与统一导出面；训练入口、对照脚本、特征重要度模块与测试全部改到新导入路径。这是纯预重构：默认模型与今天的实现逐层等价，除导入路径外不改任何行为。

## 验收标准

- [ ] 四个构建器与专家混合层由 models 包统一导出，根目录旧模型模块已删除
- [ ] 训练入口、对照脚本、特征重要度模块与测试均从新路径导入，无残留引用
- [ ] 能以模块自检方式用随机输入打印各构建器的输出形状
- [ ] 既有 19 条测试原样通过（除导入路径外不改断言）
- [ ] 默认模型的层名与参数量与今日一致

## 前置依赖

- None (can start immediately)

