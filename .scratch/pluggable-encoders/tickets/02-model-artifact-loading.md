## 父 Spec

#{{PARENT}}

## 交付内容

注册表暴露统一的加载入口（供 `load_model` 使用的自定义对象映射），特征重要度模块不再手写单个自定义层的映射；为所有自定义层补齐配置序列化与构建逻辑，使 run 目录里保存的模型能在新进程加载并复现预测。

## 验收标准

- [ ] 保存 → 新进程加载 → 同一批输入预测一致（有测试覆盖）
- [ ] 特征重要度模块通过统一入口加载模型，不再硬编码单个自定义层
- [ ] 自定义层具备配置序列化与构建逻辑，配置中包含构造超参
- [ ] 既有测试保持全绿

## 前置依赖

- #{{T1}}

