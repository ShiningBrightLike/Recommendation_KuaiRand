## 父 Spec

#{{PARENT}}

## 交付内容

新增 dcn 特征编码器：低秩矩阵交叉 2 层（默认秩 64）与一层深度分支并联，输出保留自然维度；注册后可通过 `--encoder dcn` 端到端训练、落盘、重载，并进入对照表。

## 验收标准

- [ ] `--encoder dcn` 冒烟训练通过，且该 run 的模型可重载
- [ ] 交叉为低秩形式，层数与秩来自编码器自带默认值且可覆盖
- [ ] 测试断言输出形状与参数量
- [ ] 可进入「结构+dcn」形式的对照表行

## 前置依赖

- #{{T3}}

