## 父 Spec

#{{PARENT}}

## 交付内容

新增 senet 特征编码器：按特征域做 squeeze-excite 重加权（类别特征按 embedding 维度成域、数值特征各成一域），域划分随实际特征 schema 动态推导；注册后可通过 `--encoder senet` 端到端训练、落盘、重载。

## 验收标准

- [ ] `--encoder senet` 冒烟训练通过，且该 run 的模型可重载
- [ ] 域划分随 schema 变化（例如加入影子特征后域数随之变化）
- [ ] 测试断言输出形状、参数量与域数量
- [ ] 可进入「结构+senet」形式的对照表行

## 前置依赖

- #{{T3}}

