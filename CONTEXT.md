# Recommendation_KuaiRand Context

KuaiRand 多目标反馈预测项目：基于曝光日志与用户/视频特征，同时预测点击、点赞、关注、评论四类行为，并在“特征优选”等实验性更新中持续引入新特征。

## Language

**置换重要度（permutation importance）**：
在固定模型与固定评估集上，把单个特征取值随机打乱后，各任务 AUC 相对基线 AUC 的下降量；它衡量特征对预测的边际贡献，不代表因果效应。
_Avoid_: 权重大小、SHAP 值、笼统的“特征重要性”

**总体重要度**：
单个特征在四个任务上的绝对 AUC 下降的平均值，作为跨任务排序与门控的统一口径。
_Avoid_: 按任务权重加权的混合分数、只看单个任务

**特征决策集**：
用于置换重要度计算和特征筛选的评估集；本项目固定为从训练日志按时间切出的验证集（4/16–4/21）。
_Avoid_: 测试集、把验证与测试混为一谈

**最终确认集**：
仅在候选特征组合定稿后评估一次的测试集（4/22–5/08）；每次提前使用都会消耗其确认价值。
_Avoid_: 调参集、日常对比集

**两阶段特征门控**：
候选特征的采纳流程：先在“全量模型”上按置换重要度过滤（批量阶段），再对有/无候选做同种子重训对比确认（确认阶段）；两个阶段都只看特征决策集。
_Avoid_: 前向逐步选择、仅凭阈值直接拍板

**候选特征**：
待评估是否纳入模型的新特征或实验特征。
_Avoid_: 新列、额外输入

**信号任务**：
默认参与特征门控的两个任务——点击与点赞；其正样本充足，置换重要度噪声可控。
_Avoid_: 主任务（暗示其余任务次要）

**参考任务**：
默认只展示、不参与特征门控的两个任务——关注与评论；其标签稀疏，AUC 波动大，仅供人工解读。
_Avoid_: 次任务、弱任务

**影子特征**：
在数据预处理与训练时注入的随机噪声特征（如 `shadow_0`），用于在置换重要度报告中直接估算“无信号特征”的噪声下限。
_Avoid_: 事后追加的随机列、噪声列（不经训练无法作为置换对象）
