# 特征筛选：单特征置换重要度 + 两阶段门控

候选特征筛选采用“单特征置换重要度（各任务 AUC 下降的 mean±std）+ 两阶段门控”：批量阶段按 cutoff 过滤候选，确认阶段以同种子重训对比“有/无候选”的模型，两个阶段都只看特征决策集。选择该方法而非 SHAP、权重或前向逐步选择，是因为它模型无关、与 AUC 业务口径一致、可直接用于任意已训练模型；前向选择成本高且易过拟合。方法已知局限——强相关特征间重要度摊薄、打乱后 AUC 上升产生负值——被接受并文档化，不做隐式修正。
